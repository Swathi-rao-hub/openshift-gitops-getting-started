apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
  labels:
    app: petclinic-test-app
    app.kubernetes.io/component: web
    app.kubernetes.io/instance: petclinic-test-app
    app.kubernetes.io/name: petclinic-test-app
    app.kubernetes.io/part-of: petclinic-test-app
    app.openshift.io/runtime: java
  name: petclinic-test-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: petclinic-test-app
  template:
    metadata:
      labels:
        app: petclinic-test-app
      annotations:
        revision: "gitopstestpush"
        buildDate: "20211208103658"
    spec:
      containers:
      - name: petclinic-test-app
        imagePullPolicy: Always
        image: quay.io/swaraolaxmi698/petclinic-test-repo:latest
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 45
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 1
        ports:
        - containerPort: 8080
          protocol: TCP
        - containerPort: 8443
          protocol: TCP
        - containerPort: 8778
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 45
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 5
