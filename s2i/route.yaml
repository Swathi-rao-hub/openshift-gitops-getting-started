apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: petclinic-dev-app
  name: petclinic-dev-app
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: petclinic-dev-app
    weight: 100
