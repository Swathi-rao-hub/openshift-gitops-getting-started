apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: petclinic-test-app
  name: petclinic-test-app
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: petclinic-test-app
    weight: 100
